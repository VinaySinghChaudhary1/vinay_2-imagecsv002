document.addEventListener('DOMContentLoaded', function() { const csvData = `Header 1,Header 2,Header 3\nData 1,Data 2,Data 3\nData 4,Data 5,Data 6`; const csvTable = document.getElementById('csv-table'); const rows = csvData.split('\n'); rows.forEach(row => { const cols = row.split(','); const tr = document.createElement('tr'); cols.forEach(col => { const td = document.createElement('td'); td.textContent = col; tr.appendChild(td); }); csvTable.appendChild(tr); }); const draggables = document.querySelectorAll('.card[draggable="true"]'); draggables.forEach(draggable => { draggable.addEventListener('dragstart', () => { draggable.classList.add('dragging'); }); draggable.addEventListener('dragend', () => { draggable.classList.remove('dragging'); }); }); });